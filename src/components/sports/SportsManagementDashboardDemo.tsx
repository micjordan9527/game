"use client"
import { useState } from "react"
import { Activity, AlertTriangle, CheckCircle2, CircleDollarSign, Trophy } from "lucide-react"
const modes=[{name:"稳定运行",summary:"赛事、受理与结算均在正常范围",cards:[['进行中赛事','18','正常',Activity],['待结算注单','326','正常',Trophy],['风险复核','4','关注',AlertTriangle],['今日资金处理','1,248','正常',CircleDollarSign]],action:'查看待结算与复核队列，确认没有长时间停留的记录。'}, {name:"数据异常",summary:"一场赛事数据延迟，部分市场已暂停",cards:[['进行中赛事','18','1 场延迟',Activity],['待结算注单','326','等待赛果',Trophy],['风险复核','7','增加',AlertTriangle],['今日资金处理','1,248','暂停相关结算',CircleDollarSign]],action:'优先核对数据源、影响赛事和待结算范围，再决定是否恢复市场。'}]
export function SportsManagementDashboardDemo(){const[i,setI]=useState(0);const m=modes[i];return <section className="mt-6 overflow-hidden rounded-lg border border-line bg-white shadow-sm" aria-labelledby="dashboard-demo-title"><div className="p-5 md:p-8"><div className="flex flex-wrap items-center gap-3"><h2 id="dashboard-demo-title" className="text-xl font-semibold text-ink">负责人看什么，才能判断系统是否健康？</h2><span className="rounded-md bg-paper px-2 py-1 text-xs text-muted">管理看板 · 示例数据</span></div><p className="mt-3 text-sm leading-7 text-muted">切换状态，查看指标变化该如何转成具体处理动作。</p><div className="mt-5 flex gap-2" role="group" aria-label="选择看板状态">{modes.map((x,n)=><button key={x.name} onClick={()=>setI(n)} type="button" aria-pressed={i===n} className={`rounded-md border px-3 py-2 text-sm ${i===n?'border-brand-600 bg-brand-50 text-brand-700':'border-line text-muted'}`}>{x.name}</button>)}</div></div><div className="grid gap-px border-t border-line bg-line sm:grid-cols-2 lg:grid-cols-4">{m.cards.map(([t,v,s,Icon])=>{const X=Icon as typeof Activity;return <div key={t as string} className="bg-white p-5"><p className="inline-flex items-center gap-2 text-sm text-muted"><X className="h-4 w-4 text-brand-700"/>{t as string}</p><p className="mt-4 text-3xl font-semibold text-ink">{v as string}</p><p className="mt-2 text-sm text-muted">{s as string}</p></div>})}</div><div className="p-5 md:p-8" aria-live="polite"><p className="text-xs font-semibold text-brand-700">当前判断</p><h3 className="mt-2 text-xl font-semibold text-ink">{m.summary}</h3><p className="mt-3 border-l-2 border-brand-600 pl-4 text-sm leading-7 text-muted"><CheckCircle2 className="mr-2 inline h-4 w-4 text-brand-700"/>建议动作：{m.action}</p></div></section>}
